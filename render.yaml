services:
  - type: web
    name: scrapling-mcp
    runtime: docker
    plan: free
    region: singapore
    dockerfilePath: ./Dockerfile
    dockerContext: .
    autoDeployTrigger: commit
    envVars:
      - key: PORT
        value: "8000"
      - key: SCRAPLING_MCP_AUTH_TOKEN
        generateValue: true
